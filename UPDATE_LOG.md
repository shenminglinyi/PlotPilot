# PlotPilot 更新日志

## [2026-04-14] 🚀 里程碑更新：多端点 AI 引擎动态路由与可视化配置

### 🎉 新增功能 (Features)
- **前端可视化配置面板 (Model Matrix)**：
  - 彻底告别繁琐的 `.env` 手动修改！现在可在系统左下角导航栏点击 `🧠 AI 引擎设置`，直接在网页前端所见即所得地配置 AI 供应商、API Key 与 Base URL。
  - **支持多模式切换**：
    - **🌐 统一端点配置（适合新手）**：输入一个 API Key，系统会自动探测并拉取该端点支持的所有可用模型列表，用户只需从下拉框中将高智商模型分配给“创作主力”，将快速模型分配给“分析经济”即可。
    - **🔀 独立端点配置（适合极客）**：允许用户为“主力”和“经济”模型配置完全不同的大模型供应商与端点。例如：让 OpenAI 兼容协议接入 DeepSeek 来写正文，同时让 Anthropic 接入 Claude 3.5 Haiku 来做后台低成本的数据清洗和审核。
- **真·多端点智能路由 (Multi-Model Router)**：
  - 后端引入了全新的 `MultiModelRouter`，打破了系统原本启动时“只能单提供商全局锁死”的限制。
  - 路由器现在能够根据当前的任务性质（如复杂的大纲推理 `default` vs 轻量级的摘要提取 `cheap`），将请求毫秒级无缝分发到用户在前端配置的对应端点和供应商实例中。

### 🛠️ 优化与重构 (Refactor)
- **底层架构解耦**：移除了业务代码（如 `SceneDirectorService`, `TensionAnalyzer` 等）中大量硬编码写死的 Anthropic 模型名称，全面重构为读取 `Settings().default_model` 和 `Settings().cheap_model` 的动态绑定模式。
- **动态配置持久化**：新增 `DynamicSettingsManager`，用户的可视化配置项会安全地持久化存储于 `data/llm_config.json` 中，确保后台托管写书的守护进程（Daemon）能够与前端界面的模型配置保持同步，并且脱敏保护了下发给前端的 API 凭证。
- **平滑兼容**：虽然实现了全新的动态路由层，但在用户未进行前端配置时，系统依旧能够完美向后兼容读取旧版的 `.env` 配置并正常启动。